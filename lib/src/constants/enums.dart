enum AnswerType {
  shortText,
  multipleChoice,
  contactInfo,
  address,
  phoneNumber,
  longText,
  yesNo,
}

enum ImageLayout {
  inBetween,
  right,
  left,
  rightExpanded,
  leftExpanded,
  background
}
