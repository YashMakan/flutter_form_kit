enum AnswerType {
  shortText,
  multipleChoice,
  singleChoice,
  contactInfo,
  address,
  phoneNumber,
  longText,
  yesNo,
}

enum ImageLayout {
  inBetween,
  right,
  left,
  rightExpanded,
  leftExpanded,
  background
}
